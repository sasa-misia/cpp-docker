#include <cstdio> // c standard input output library
using namespace std;

// we define the main function and we want an integer output (0) if code run correctly
int main(){
    puts("Click on Button");
    puts("Create a New Player");
    puts("Add Life to Player");
    return 0;
}

// we define a function that returns nothing, so we do not need the statement return
void nothing(){
    puts("Hello world");
}

// a new function that returns character (chars are with single quote, not double)
char iamachar(){
    return 'h';
}

/*
this is an
example
of a 
multiline
comment
*/